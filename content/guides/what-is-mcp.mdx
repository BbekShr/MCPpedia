---
title: "What is MCP? A Beginner's Guide"
description: "The Model Context Protocol (MCP) is an open standard that lets AI assistants connect to external tools and data sources. Here's everything you need to know."
author: "MCPpedia"
date: "2026-03-01"
tags: ["beginner", "introduction"]
---

## What is the Model Context Protocol?

The **Model Context Protocol (MCP)** is an open standard created by Anthropic that enables AI assistants like Claude to connect with external tools, data sources, and services. Think of it as a universal adapter — instead of building custom integrations for every tool, MCP provides a single protocol that works everywhere.

## Why does MCP matter?

Before MCP, connecting an AI assistant to your tools required custom code for each integration. Every application had its own way of doing things. MCP changes this by providing:

- **A standard interface** — one protocol for all tools
- **Security by default** — users control what the AI can access
- **Composability** — mix and match servers for your workflow

## How MCP works

MCP follows a simple client-server architecture:

1. **MCP Servers** expose tools, resources, and prompts. For example, a GitHub MCP server might expose tools like `create_issue` or `search_repositories`.

2. **MCP Clients** (like Claude Desktop, Cursor, or Claude Code) connect to these servers and make the tools available to the AI.

3. **Transport** — servers and clients communicate via stdio (local) or HTTP/SSE (remote).

## Key concepts

### Tools
Tools are functions the AI can call. Each tool has a name, description, and input schema. For example:

```json
{
  "name": "search_files",
  "description": "Search for files matching a pattern",
  "input_schema": {
    "type": "object",
    "properties": {
      "pattern": { "type": "string" }
    },
    "required": ["pattern"]
  }
}
```

### Resources
Resources are data the AI can read — like files, database records, or API responses. They're identified by URI templates.

### Prompts
Prompts are pre-written templates that help the AI use a server's capabilities effectively.

## Getting started

Ready to try it out? Check out our guide on [how to install your first MCP server](/guides/install-first-server), or browse the [MCPpedia directory](/servers) to find servers for your workflow.
