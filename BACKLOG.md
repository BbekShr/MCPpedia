# Backlog

The org's work queue — one of the three memory stores (see `docs/org-memory/README.md`).

**Bright line:** improvement cycles may ONLY change a row's Status, append notes to it, and
append new rows. Priority and acceptance criteria are the human's lever — cycles never edit
them and never delete rows. Status values: `open` · `in-progress` · `in-review` · `done`.

ID taxonomy mirrors the standing goals: **S** = stability/features/bugs, **R** = refactors,
**W** = keep-current/dependency drift, **M** = org/process improvements. Priority P1 (urgent)
→ P4 (someday). Protected = the change will touch a protected path (CLAUDE.md §5), so its PR
needs the human-approved label.

| ID | P | Item | Acceptance criteria | Status | Protected |
|----|---|------|---------------------|--------|-----------|
| S1 | P2 | CI has no build gate — `next build` never runs in CI, so type-safe-but-unbuildable changes (bad route exports, RSC violations, config errors) surface only at Vercel deploy time | `.github/workflows/ci.yml` runs `npm run build` on every PR (with placeholder env vars as needed) and it passes on main | in-review | yes |
| S2 | P3 | `npm run lint` reports 11 warnings (unused vars etc.) — warning noise hides new ones | `npm run lint` exits with 0 errors and 0 warnings, with no `eslint-disable` added and no rule weakened | in-review (10/11 fixed; PR touches `lib/__tests__/**` so needs human-approved. Item #1 `app/admin/page.tsx:245` is NOT an unused directive — removing it surfaces 3 real `set-state-in-effect` errors; the effect refactor to reach 0/0 is filed as S7) | no (but PR touches protected `lib/__tests__/**`) |
| S3 | P3 | No smoke check exists — nothing verifies the built app boots and serves pages | A scripted smoke check (build + `next start` + curl of `/`, one server page, and one API route asserting HTTP 200) exists as an npm script, is documented in CLAUDE.md §2, and runs in CI | open (note 2026-07-19: the servers-listing incident showed the acute gap is a POST-deploy smoke check against *prod* — this CI pre-deploy check runs against a mock client and would not have caught a manually-unapplied migration or a prod-only statement timeout; consider a prod-target variant, see M2) | yes |
| M1 | P3 | Pre-existing `.claude/commands/qa.md` predates the org and overlaps qa-verifier | A human decision is recorded in `docs/org-memory/codebase.md`: fold it into qa-verifier's feature-specific checks, keep it as a standalone manual skill, or delete it | open | no |
| W1 | P4 | Dependency drift is untracked — Next.js 16 / React 19 / Supabase SDKs move fast and this repo pins a breaking-changes Next version | A dated drift report appended to `docs/org-memory/codebase.md`: `npm outdated` summary, upstream breaking changes that affect this codebase, and any W rows filed for needed migrations | open | no |
| S4 | P3 | Env-less CI build prerenders 701 `/compare/[slugs]` pages that all render `notFound()` — `app/compare/[slugs]/page.tsx:40-47` lacks the env guard that `app/s/[slug]/page.tsx:98-101` has, so with the mock client every pair 404s (~15–40s of CI time producing 701 useless 404 pages) (found in S1 perf review) | Compare's `generateStaticParams` returns `[]` when Supabase env is absent (mirroring `s/[slug]`), or an equivalent guard; env-present builds still prerender all pairs; the ~700 build-time 404s no longer occur | in-review | no |
| R1 | P4 | The two Supabase mock clients diverge — `lib/supabase/server.ts` mock lacks `maybeSingle/gt/gte/lt/lte/in/not/ilike/overlaps` that `lib/supabase/public.ts` has; a future build-time-reachable server-client caller using a missing method would TypeError only in the env-less CI build (found in S1 correctness/regression review) | The two mocks expose an identical method surface (shared factory or aligned method set) so env-less builds cannot fail on a method the real client supports; note: touches `lib/supabase/**` (protected) | open | yes |
| S5 | P4 | CI has no `.next/cache` persistence and no `concurrency` cancel-in-progress — every PR is a cold Turbopack build (~2–4 min) that re-downloads Google Fonts, and rapid pushes run redundant full builds (found in S1 perf review; deferred by CEO decision that cycle) | `.github/workflows/ci.yml` caches `.next/cache` keyed on lockfile+source and adds a `concurrency` group with `cancel-in-progress: true` for the CI workflow; build time drops on warm cache and superseded runs cancel | open | yes |
| S7 | P3 | `app/admin/page.tsx:245` carries a `// eslint-disable-next-line react-hooks/exhaustive-deps` that lint reports as "unused" but is load-bearing: removing it surfaces 3 `react-hooks/set-state-in-effect` errors (`:223 refreshPendingEdits()`, `:233 fetchNonServerData()`, `:239 setLoading(true)`) in sibling effects. This is the last of the S2 lint warnings and can't be closed by dead-code removal (found in S2) | The three admin data-fetch effects no longer call setState synchronously (refactored so the work happens outside synchronous effect execution), the `:245` eslint-disable is removed, `npm run lint` exits 0 errors / 0 warnings, no new `eslint-disable` and no rule weakened, and admin tab behavior is unchanged (pending edit/claim counts still load, server-search debounce still works) | open | no |
| S6 | P3 | Publisher-claim verification is manual only — the MVP (`app/api/claim`, `app/api/admin/approve-claim`, claims tab) requires a maintainer to eyeball each proof, and there is no claimant notification (notifications.type CHECK is limited to `edit_approved`/`edit_rejected`, so adding claim events needs a migration) | Automated proof verification for at least `github_repo`/`github_org` (issue a `mcppedia-<token>`, fetch GitHub to confirm, auto-verify), plus a `claim_approved`/`claim_rejected` notification type (migration) so claimants are told the outcome | open | yes |
| S8 | P2 | `home_stats_cache` has been frozen since 2026-07-05 — the homepage hero and `/security` show `total_servers` = 20,499 and a July-5 `last_security_scan` while the catalog is ~32.4k active (46k total). Root cause: `refresh_home_stats_cache()` now exceeds its statement timeout. The catalog roughly doubled (~20k→46k rows) in a ~July-5 import and the function's two full-table FILTER-aggregate scans over `servers` (a plain full count alone is ~5.3s) can no longer finish; calling the RPC today returns 500. The daily `bots/compute-scores.ts:256-267` refresh swallows the error with `console.warn` (its own comment still assumes "~20k servers"), so the staleness is silent and unmonitored. `daily_metrics` and score computation are unaffected. (found investigating the analytics truncation bug, PR #47) | `refresh_home_stats_cache()` completes within the statement timeout at current table size — via a supporting approach (e.g. `SET LOCAL statement_timeout` inside the SECURITY DEFINER function, `reltuples` estimate for `total_servers`, indexing, or incremental maintenance) — the homepage hero and `/security` reflect live totals after a compute-scores run, and the refresh failure is surfaced (fails the bot step or emits an alert) rather than only `console.warn`. Touches `supabase/migrations/**`. | in-review (fix in this PR: migration `20260718120000` raises `service_role` statement_timeout to 120s — an in-function `SET` is a no-op because the timer arms before entering the function; `bots/compute-scores.ts` now logs at error level + sets a non-zero exit on refresh failure. Needs deploy-time verification that the refresh completes and the homepage self-corrects) | yes |
| S9 | P3 | Wrongly-archived imports stay permanently hidden — `update-metadata` is archive-forward only (sets `is_archived: true` when a repo goes archived but never clears it when the repo is live again), so an import that was archived at ingest, or archived upstream then un-archived, never reappears in search. Hit GitHub issues #22 (TheCrawler was live but ingested archived) and #26 (kinetic-gain live but shown archived); both needed manual DB fixes. `bots/extract-schemas.ts` also skips `is_archived` rows, so these listings also never get tools | `update-metadata` re-reads GitHub `archived` and clears `is_archived` (sets health_status off archived) when the upstream repo is no longer archived, so a repo going live again restores its listing without manual intervention; verified against a currently-mislabeled row | open (2026-07-25: a first attempt was built and REJECTED by data-integrity review — do not re-attempt with the same design. Three independent fail-OPEN holes were found in the proposed `canUnarchive` gate: (1) 'no `server_changes` audit row means archived at INSERT' is FALSE — `bots/detect-duplicates.ts` and its weekly cron shipped 2026-04-03 (`6a9c806`) while the audit trigger only landed 2026-04-16 (`dd78ba0`), so the 04-06 and 04-13 merge runs archived duplicates with zero audit rows; (2) the live-sibling check used an unbounded `.ilike('%/owner/repo%')`, which PostgREST silently truncates at 1000 rows, so a truncated read reads as 'no sibling' — and the substring pattern matches far more than the exact URL for monorepo paths; (3) a renamed repo lets the gate mint a duplicate pair whose `github_url`s have diverged, which `detect-duplicates` can then never group. Also NOTE this row's claim that '`bots/extract-schemas.ts` also skips `is_archived` rows' is WRONG — that query filters only on `github_url IS NOT NULL` and `tools = '[]'`. Any future attempt must also cover S52, which bypasses and corrupts the audit history such a gate reads) | no |
| S10 | P3 | No cron detects an upstream repo rename/redirect — when a listing's `github_url` 301-redirects to a new repo (renamed/moved), nothing updates the stored URL, homepage, or package, so the listing sends developers to stale repo names and install paths. Hit GitHub issue #23 (hive-crypto-mcp → canonical hive-intel/hive-sdk); needed a manual repoint | A cron (or `update-metadata` extension) follows the GitHub 301 for each listing's `github_url` and, on a stable redirect, updates `github_url`/`homepage_url` to the canonical target (leaving name/package for human review or refreshing them from the new repo metadata); verified on a known-redirecting row | open | no |
| S11 | P3 | `lib/sitemap-shared.ts:fetchServerChunk` paginates the `servers` table with offset-based `.range()` (same pattern that timed out `/analytics` at ~19k rows, fixed in this PR via keyset pagination). It's currently ordered by an indexed `(score_total, slug)` composite for SEO rank, which is why a naive single-column keyset swap wasn't done here — a correct fix needs a composite cursor (`score_total < cursor OR (score_total = cursor AND slug > cursor_slug)`, with `NULLS LAST` handled) to preserve sitemap ordering exactly. Not yet observed to fail in CI/local builds, but same growth trajectory as `/analytics` (found while fixing the analytics timeout) | `fetchServerChunk` no longer uses OFFSET `.range()` for the per-chunk walk — keyset/cursor pagination on `(score_total, slug)` (or an equivalent index-seek approach) — and sitemap URL ordering by score rank is unchanged (verified against current sitemap output) | open | no |
| S12 | P1 | The S11-adjacent `/analytics` keyset fix (PR #51) was not sufficient on its own — a Vercel build still failed with `canceling statement due to statement timeout` at a reproducible cursor, blocking every deploy off `main` since keyset pagination fixes the O(n²) offset-scan cost but not per-page payload weight. Root cause: `app/analytics/page.tsx` selected the full `tools` jsonb (complete per-server MCP tool schemas) on every 1000-row page just to sum `tools.length` — the heaviest column in the table, fetched over the **anon** role, which (per `20260718120000_home_stats_refresh_timeout.sql`'s own comment) never got the `service_role` statement_timeout raise; a plain full-table count is already ~5.3s at the current ~46k-row (~32k active) size | `app/analytics/page.tsx` no longer selects `tools`; `totalTools` sources from the already-fetched `daily_metrics.total_tools` instead. Touches no protected path. Status `done` once a production Vercel build off `main` completes without an `/analytics` prerender error (could not be verified locally — the timeout is data-size/anon-role-specific and didn't reproduce against a local connection) | in-review (fix pushed as its own PR, not #51/#52 — needs deploy-time verification) | no |
| S13 | P1 | Admin audit-trail loss — `archive`/`verify` routes insert an `edits` audit row with `status:'approved'` via the user-scoped client, but the `edits` INSERT policy (20260610000000) only permits `status='pending'`, so RLS silently rejects every insert and the unchecked error is swallowed; no admin archive/verify has been audited since 2026-06-10 (PLATFORM_REVIEW §1.1) | Audit inserts go through the service-role admin client (bypasses RLS) and any error is logged, not swallowed; no schema/policy change | in-review (PR #57) | no |
| S14 | P1 | Broken filtered pagination on `/servers?q=` — `min_score`/`transport`/`author` filters run in JS after the search RPC paginates, causing under-filled pages, wrong `hasNextPage`, and rows vanishing between pages (PLATFORM_REVIEW §1.2) | The three filters are pushed into `search_servers` as optional params so filtering precedes pagination; post-fetch JS filtering removed; non-search branch unchanged | in-review (PR #58) | yes |
| S15 | P2 | Card/metadata reads ship the full `tools` JSONB (heaviest column) just to render a count — largest egress line on the shared DB and a repeat build-timeout cause (PLATFORM_REVIEW §2.3/§2.1) | `tool_count` STORED generated column added; `PUBLIC_CARD_FIELDS`, similar-servers, detail `generateMetadata`, and OG image read it; full `tools` kept only where the detail body renders the tool list | in-review (PR #59) | yes |
| S16 | P2 | 13 API routes have no rate limit incl. the unauthed v1 firehose (CORS `*`, uncapped offset, full tools JSONB) (PLATFORM_REVIEW §1.5/§1.6/§1.8) | Rate limits added to favorites/github-metadata/server[slug]/refresh-score/v1; v1 offset capped at 10k and heavy tools/resources/prompts dropped from the list payload; favorites UUID-validates + stops leaking raw DB errors | in-review (PR #61) | no |
| S17 | P2 | Untested CVSS engine behind the Security score — `computeCVSS3BaseScore` unexported and untested; scoring tests pass `null` packages so the CVE path never runs (PLATFORM_REVIEW §1.3) | `computeCVSS3BaseScore` exported and pinned with `parseCVSSScore`/`cvssToSeverity` against official CVSS 3.1 reference vectors (23 assertions); 120/120 tests | in-review (PR #60) | yes |
| S18 | P2 | Health status manipulable by one user — `health_checks` rows carry no `user_id`; last-5 majority lets 5 `fail` reports from one account flip a healthy server (PLATFORM_REVIEW §1.4) | `user_id` added to `health_checks`; status/uptime recomputed from one vote per distinct user (latest report), majority over 5 most-recent distinct voters | in-review (PR #62) | yes |
| S19 | P2 | No observability — bot/deploy failures only email the last committer; `home_stats_cache` froze silently for two weeks (S8) (PLATFORM_REVIEW §2.5) | Freshness probe (every 6h, fails when `home_stats_cache.refreshed_at` >48h stale) + fleet-wide `alert-on-failure` workflow opening a deduped `bot-failure` issue on any scheduled bot/probe failure; Sentry left as a follow-up (needs DSN) | in-review (PR #63) | yes |
| S20 | P1 | Servers-listing statement-timeout outage (2026-07-19) — `/servers`, `/api/v1/servers`, and large `/category/*` 500 with `canceling statement due to statement timeout` because the listing query ran `count: 'exact'` over the ~46k-row `servers` table, exceeding the anon per-statement timeout at current size; the live production outage that triggered this incident | Listing endpoints use an estimated/planner count (`count: 'estimated'` / `reltuples`) instead of `count:'exact'` so a page render never runs a full-table count; `/servers`, `/api/v1/servers`, and large `/category/*` return 200 under prod load; pagination totals stay accurate enough for the UI | done (PR #65 merged; code-only, live on deploy — no migration to apply) | no |
| S21 | P2 | Duplicate `search_servers` overload landmine — `20260719120000` added a 10-arg `search_servers` via `create or replace`, which did NOT replace the old 7-arg function (Postgres keys on full signature), leaving two overloads; a 7-arg rpc call then raises `function search_servers(...) is not unique` and 500s `/api/search`. Guaranteed on a fresh in-order migration apply; hot-patched by hand in prod on 2026-07-19 | A migration drops the stale 7-arg overload idempotently so the schema converges to a single 10-arg function on any fresh apply; `/api/search` resolves unambiguously; the surviving 10-arg defaults keep 7–10-arg callers working. Touches `supabase/migrations/**` | done (PR #66 merged) — ⚠️ migration file merged but NOT auto-applied; a human must run it on prod (no-op there, overload already dropped by hand); value is fresh-apply correctness (see M2) | yes |
| S22 | P3 | `/analytics` build-time fetch throws instead of degrading — `app/analytics/page.tsx` throws on any page-fetch error (and on a >50% sanity-guard shortfall), so a single transient shared-DB hiccup at build fails the whole deploy. The acute statement-timeout cause is already mitigated (service_role client + keyset pagination, S12); the resilience half of PLATFORM_REVIEW §2.1 fix 2 (degrade → serve last-known-good) is unshipped | On a transient fetch failure the analytics build-time fetcher degrades to last-known-good (a bot-committed cached snapshot or the previous ISR output) rather than throwing and blocking the deploy; a genuine data-truncation still refuses to cache bad numbers (existing guard preserved) | open | no |
| M2 | P2 | Migrations have no post-merge apply step and the org record wrongly assumed they auto-apply on deploy — there is NO deploy-time migration runner (build is plain `next build`; nothing runs `supabase db push`), so a merged migration file is not live until a human applies it by hand. This assumption directly caused the 2026-07-19 servers-listing incident (fixes merged but not applied); corrected in `docs/PLATFORM_REVIEW_2026-07.md`. Separately, the Supabase MCP connected to the agent session points at the Pasa project, not MCPpedia, so migration state can't be verified from a session | A migration-apply runbook is documented (CLAUDE.md §2 or `docs/org-memory/`) stating exactly how/when a merged migration reaches prod and how to verify it is live; a decision is recorded on whether to automate it (a guarded `supabase db push` step, or a CI check that flags migration files on `main` not yet applied); the "migrations don't auto-apply" fact is reflected wherever the org reasons about deploys | open | maybe (CLAUDE.md §2 and any workflow-based apply step are protected paths) |
| S23 | P1 | Privilege escalation via a stale duplicate RLS policy on `profiles` — `20260404010000_restrict_profile_role_update.sql:7` created `"Users can update own profile except role"` (WITH CHECK freezes ONLY `role` and `created_at`), and `20260610000000_security_hardening.sql:55` then `DROP POLICY IF EXISTS "Users can update own profile"` (a name that no longer existed) before re-creating it with the karma/counter freeze. Postgres ORs permissive policies for the same command, so the weaker 20260404 policy still permits any signed-up account to `PATCH /rest/v1/profiles?id=eq.<self>` with `{"edits_approved":99,"karma":999999}`. `edits_approved` is trigger-maintained and never recomputed, so the forged value persists and clears the auto-approve trust gate at `app/api/edit/route.ts:54` (threshold 3, `lib/validators.ts:48`) — every subsequent low-risk edit then writes straight to `servers` through the service-role client with no moderator review. Same class as S21: a later migration re-`CREATE`d a renamed object instead of replacing it (found 2026-07-24 discovery, api-security lens) | An idempotent migration drops the stale `"Users can update own profile except role"` policy so `pg_policies` for `tablename='profiles' AND cmd='UPDATE'` returns only the hardened self-update policy plus `"Admins can update any profile"`; an authenticated non-admin PATCHing their own `karma` or `edits_approved` is rejected while legitimate self-updates (bio/username) still succeed; prod verification is recorded per M2. Touches `supabase/migrations/**` | open | yes |
| S24 | P1 | `bots/snapshot-metrics.ts:17-34` silently persists a truncated catalog into `daily_metrics` — the fetch loop swallows the page `error` (`const { data } =`, no `error`), selects the heavy `tools` jsonb (the exact column S12 removed from `/analytics` for exceeding the statement timeout), and walks offset `.range()` with NO `.order()` so rows can duplicate and skip even on success. On a timeout at page 7 of 32 the loop `break`s, `total_servers`/`total_tools`/every average and distribution bucket are computed off that slice, the upsert (`:195-199`, onConflict `snapshot_date`) writes it, and `bot_runs` records SUCCESS. `app/analytics/page.tsx:551-575` reads the last 90 `daily_metrics` rows for its trend charts and the ~7-day-ago row for every delta arrow, so one truncated day permanently poisons the trend and can invert every week-over-week delta. The app-side twin (`app/analytics/page.tsx:517-537`) already uses keyset pagination, excludes `tools`, and throws on a page error. Same swallowed-error pattern in the four `bots/discover.ts:45-112` dedupe-set loaders, where a truncated set is the ONLY duplicate guard (see M4) (found 2026-07-24 discovery, bots data-integrity lens) | `snapshot-metrics` paginates by keyset on `id`, does not select `tools`, and THROWS on any page error so the run turns red instead of upserting a partial row; a sanity guard refuses the upsert when `total_servers` is more than 20% below the previous snapshot; the four `discover.ts` dedupe loaders check `error` and throw; a test feeds a fake client that errors on page 2 and asserts no `daily_metrics` upsert and a non-zero exit | open | no |
| S25 | P1 | On an OSV scan failure `bots/compute-scores.ts:163-164` writes `score_security = 0` for never-successfully-scanned servers and can write `score_total > 100`. Both fallbacks (`server.score_security ?? security.score`, `?? 0`) assume "never scanned means null", but `score_security`/`score_total` are `integer default 0` NOT NULL (`supabase/migrations/20260402010000_scores_security_registry.sql:6-7`), so the `??` never fires: a fresh registry import whose OSV query 429s gets `score_security = 0` and `score_total = total - security.score + 0` — up to 30 points below truth — and `lib/mcp/tools.ts:136-143` renders that as "CRITICAL RISK — Security score: 0/30". `score_computed_at` is still stamped and the loop is stalest-first (`:51`), so the deflated row goes to the BACK of the queue and persists for days; a global OSV outage deflates every server in that run. Conversely, when a prior score exists and exceeds the fresh one, `total` was already clamped to 100 at `:147` before the swap, so `100 - 15 + 30 = 115` is writable, breaking the 0-100 contract for exactly the top-ranked servers (found 2026-07-24 discovery, scoring correctness lens) | The OSV-failure merge is extracted as a pure helper in `lib/` and unit-tested: a server with no successful prior scan (`last_security_scan` null or `security_scan_status` not 'success') keeps the fresh non-CVE security component rather than falling back to 0 (or the row is left untouched so it stays at the head of the stalest-first queue), and `score_total` written by the bot is always within 0-100 | open | no |
| S26 | P1 | Open CVEs with unparseable severity — including EVERY CVSS-v4-only advisory — incur ZERO score penalty while the UI still reports "N open CVEs". `lib/scoring.ts:117` accepts `type === 'CVSS_V4'` and `:126` routes any `CVSS:`-prefixed vector to `computeCVSS3BaseScore`, which reads `C`/`I`/`A` (`:87-89`) — metrics that do not exist in CVSS 4.0 (it uses `VC`/`VI`/`VA`) — so the null-guard at `:93` fires, `cvssToSeverity(null)` returns `'info'` (`:134`), and `checkCVEs`'s penalty (`:237`, `critical/high*5 + medium*3 + low*1`) buckets nothing, awarding a full 15/15. Three unfixed CVSS-4.0 criticals therefore score identically to a clean package, with detail text "3 open CVE(s): 0 critical/high, 0 medium, 0 low". The `find` at `:117` is array-order dependent, so an OSV record listing `CVSS_V4` before `CVSS_V3` also discards the parseable v3 vector. Same path for `CVSS_V2`-only and severity-less advisories (found 2026-07-24 discovery, scoring correctness lens) | `checkCVEs` penalizes an open advisory of unknown/`info` severity non-zero (an explicit "unrated" bucket or the `medium` weight), and `parseCVSSScore` either computes CVSS 4.0 correctly or prefers a `CVSS_V3` entry over an unparseable `CVSS_V4` one; tests assert that 3 open CVSS-4.0 advisories yield a `cve` evidence `points < 15` with severity not `'info'`, and that a mixed `[CVSS_V4, CVSS_V3]` severity array parses to the v3 base score. Touches `lib/__tests__/**` | open | yes |
| S27 | P1 | Deep-offset pagination on `/servers` crosses the 3s anon statement timeout and serves a 200-OK EMPTY catalog — verified live in prod on 2026-07-24: `?page=1000` and `?page=2000` return HTTP 200 in ~3.4s with zero cards and "No servers found", and `?page=500` flapped (3.28s empty cold, 0.80s correct on retry). S20 removed `count:'exact'` from this query but left the unbounded OFFSET walk at `app/servers/page.tsx:111` (`page` has no upper bound). On error `servers = []` and `totalCount = 0` hides the pagination while the headline still shows 39,409 because it comes from the separate `home_stats` RPC (`:130`), so a timeout renders as a SUCCESSFUL empty catalog. Reachable by any crawler: the estimated count emits ~1,970 Prev/Next anchors and page 600 links to 601, and `/servers` is priority 0.9 in `lib/sitemap-shared.ts:57` (found 2026-07-24 discovery, pages performance lens) | `/servers` no longer performs an unbounded OFFSET walk — either the page number is clamped (deep pages noindex plus 404/redirect above a bounded max, e.g. offset 10k matching the S16 v1 cap) or the listing uses keyset pagination on the sort key; `curl /servers?page=1000` and `?page=2000` return a bounded 404/redirect or a page with 20 cards, never a 200 with "No servers found"; a genuine query error renders an explicit degraded state instead of an empty catalog; a test pins the page-to-offset clamp | open | no |
| S28 | P1 | `/badge` is prerendered once per deploy and frozen forever, publishing "MCPpedia tracks 17,000 MCP servers" while `/about` on the same day renders 39,410. `app/badge/page.tsx:83-87` runs an anon `count:'exact'` full-table count, discards the `error`, and falls back to a hardcoded `|| 17000`; the file exports NO `revalidate`, so prod serves `x-vercel-cache: HIT` with `age: 452489` (5.2 days) and can never self-correct. The count evidently failed at build time (its sibling query in the same file succeeded), i.e. the S20 outage class on an unaudited page. `/badge` is sitemap-listed (`lib/sitemap-shared.ts:62`) and is the destination of the badge-embed CTA on ~39k detail pages, advertising a catalog 2.3x smaller than reality. `app/about/page.tsx:17-20` has the same unchecked full-table `count:'exact'` and would render "tracking 0 servers" pinned for 24h by its `revalidate = 86400` (found 2026-07-24 discovery, pages performance lens) | Neither `/badge` nor `/about` issues a `count:'exact'` against `servers`; both read the total from the shared `home_stats` snapshot (as `app/servers/page.tsx:130` does); `/badge` exports a `revalidate` so it refreshes without a deploy; the `|| 17000` hardcoded fallback is removed and the query `error` is logged; after deploy both pages show a total within one refresh window of `home_stats.total_servers` | open | no |
| S29 | P1 | Sitemaps cover only 30,000 of 39,409 non-archived servers — ~9,400 detail pages are in NO sitemap. `lib/sitemap-shared.ts:8` sets `SERVER_CHUNK_SIZE = 10000` and exactly three hardcoded chunk routes exist (`app/sitemap-servers-{1,2,3}.xml/route.ts`, registered in `app/sitemap.xml/route.ts`); prod `<loc>` counts are 10,000/10,000/10,000, so chunk 3 is saturated and everything ranked below position 30,000 by `score_total desc` is invisible to crawlers. The gap was zero at ~20k rows and grows 1:1 with the catalog. Distinct from S11, which only changes HOW each chunk paginates (found 2026-07-24 discovery, SEO performance lens) | Total `<loc>` entries across `/sitemap-servers-*.xml` equals the non-archived server count (within one refresh window); the chunk count is derived from the row count rather than hardcoded to 3, so no manual edit is needed at the next 10k of growth; a test mocking a 39,409 total asserts the index route enumerates at least 4 chunks | open | no |
| S30 | P2 | `/api/search` and `/api/mcp` (`action:"search"`) return EVERY column of `servers`, bypassing the public field allow-list. `search_servers` is declared `returns setof servers` (`supabase/migrations/20260719120000_search_servers_filters.sql:21`) and both callers serialize the rows verbatim — `app/api/search/route.ts:31` (unauthenticated, `s-maxage=900` CDN cache) and `app/api/mcp/route.ts:183` (only its ilike FALLBACK branch projects `SEARCH_FIELDS`, which shows the intent). Leaked columns absent from `PUBLIC_SERVER_FIELDS` (`lib/constants.ts:112`, documented as excluding "internal scoring details, scan internals, claimed_by, etc.") include `submitted_by` and `claimed_by` (auth.users UUIDs, never rendered anywhere in the app), `security_issues`, `security_verified`, `data_quality`, `registry_id`/`registry_synced_at`/`registry_verified`, `dangerous_pattern_count`, `dep_health_score`, the `fts` tsvector, and the full `tools`/`resources`/`prompts` JSONB. Because `profiles` is world-readable by `id`, `submitted_by` de-anonymizes every submission; the jsonb payload also re-introduces the heavy-egress pattern S12/S15/S16 removed (found 2026-07-24 discovery, api-security lens) | Both routes project the RPC result through an explicit allow-list before responding (or `search_servers` returns a narrowed row type); `submitted_by`, `claimed_by`, `fts`, `security_issues`, `security_verified`, `data_quality` and `registry_*` never appear in either response body; a test asserts the search response key set is a subset of the public field list so a new `servers` column cannot start leaking; fields existing card consumers read are unchanged | open | no |
| S31 | P2 | `/api/admin/approve-edit` permits SELF-approval, so one `editor` account can push the identity/install fields the design says must always queue. The route admits role `editor` (`app/api/admin/approve-edit/route.ts:31`) and never compares `edit.user_id` with `user.id` — that comparison exists only to decide whether to send a notification (`:70`, `:132`). Its `ALLOWED_FIELDS` (`:10-13`) include `name`, `homepage_url`, `npm_package`, `pip_package`, which `LOW_RISK_FIELDS` deliberately excludes because "reverting a swapped npm package leaves dead links and broken installs in the wild" (`lib/validators.ts:36-42`). The only DB-side defence is the INSERT policy pinning `status='pending'`, labelled "prevent self-approval farming" (`20260610000000_security_hardening.sql:24-34`) — a two-step insert-then-approve by the same account walks through it and the write lands via the service-role client (`:107-111`) (found 2026-07-24 discovery, api-security lens) | `/api/admin/approve-edit` returns 403 when `edit.user_id === user.id`, unless the approver's role is in an explicitly higher set (record the human decision on whether `maintainer`/`admin` may self-approve); self-REJECTION still works; tests cover editor approving own pending `npm_package` edit (403, `servers.npm_package` unchanged) and editor approving another user's edit (200 as today) | open | no |
| S32 | P2 | `bots/detect-duplicates.ts` can archive a live, non-duplicate server — including the keeper itself. The fetch orders by the NON-UNIQUE `data_quality` (`:86`; `integer default 0`, so tie groups are enormous) and `fetchAllRows` walks it with offset `.range()` pages (`bots/lib/supabase.ts:49`), so a row can be returned on two consecutive pages; `bots/compute-scores.ts:52` adds `.order('id')` with the comment "stable tiebreak for pagination", this bot does not. The merge loop (`:118-141`) then treats `group[0]` as keeper and every other element as a dupe with NO `dupe.id === keep.id` guard, so a doubly-returned row becomes its own duplicate: `reparent` is a self no-op and `update({is_archived:true}).eq('id', dupe.id)` archives THE KEEPER. The listing vanishes from search, `/servers`, sitemaps and category pages; `update-metadata` is archive-forward only (S9) so it never returns, `extract-schemas` skips archived rows, the log prints KEEP and MERGE for the same slug, and the run reports success (found 2026-07-24 discovery, bots data-integrity lens) | The fetch adds a unique tiebreak (`.order('id')`) so pagination is deterministic; the merge loop dedupes each group by `id` before comparing length and skips any `dupe.id === keep.id`; a test feeds a row set containing the same `id` twice under one `github_url` and asserts zero `is_archived: true` writes | open | no |
| S33 | P2 | `security_advisories` is append-only — stale advisories stay `open` forever and one vulnerability can occupy two rows. `bots/compute-scores.ts:207-228` upserts the advisories the fresh OSV scan returned on `server_id,cve_id`; nothing anywhere deletes a row or marks one fixed/withdrawn when it stops appearing (the only other writer is the one-off `scripts/apply-classifications.ts:263`). (a) When `bots/extract-install-info.ts:185` legitimately clears a wrong `npm_package`, the next scan finds nothing so `cve_count`/`score_security` recover, but the old rows stay `status:'open'` — still shown on the detail page and the homepage advisory feed, and still inflating `open_cves`/`cves_critical` in every future `daily_metrics` snapshot (`bots/snapshot-metrics.ts:66-79`) and on `/security`. Same for a withdrawn OSV entry or an upstream package rename. (b) `cve_id` is `v.aliases?.find(a => a.startsWith('CVE-')) || v.id` (`lib/scoring.ts:189`), and OSV routinely adds a CVE alias to a GHSA advisory weeks later — so the same vulnerability lands as `GHSA-xxxx` and later as `CVE-2026-xxxx`, both counted and displayed as open (found 2026-07-24 discovery, bots data-integrity lens) | After each per-server scan, advisories in the DB for that `server_id` absent from the fresh result are reconciled (marked fixed/withdrawn or deleted) and ONLY when `scan_status === 'success'`, never on the `osvFailed` path (`bots/compute-scores.ts:157`); advisory identity is stable across alias changes (key on the OSV `id`, keep `cve_id` for display); tests cover an advisory disappearing from a scan no longer counting as open, and the same OSV `id` re-scanned after gaining a CVE alias producing one row not two | open | no |
| S34 | P2 | `/api/server/[slug]/refresh-score` and `/api/submit` overwrite CVE-derived columns on a FAILED OSV scan — the exact thing `bots/compute-scores.ts:155-157` documents as forbidden ("skip CVE-derived columns to avoid overwriting good data with an inflated 'no CVEs found' result from a transient API outage"). `app/api/server/[slug]/refresh-score/route.ts:152-166` and `app/api/submit/route.ts:229-241` write `score_security`, `cve_count`, `security_evidence` and `score_total` unconditionally, never inspecting `security.scan_status`. A maintainer refreshing a server with 2 real open criticals while OSV is rate-limiting gets `checkCVEs` seeing zero advisories (15/15, "No known CVEs"), so the route writes `cve_count: 0` and the inflated `score_security`, destroys the good data, leaves `security_advisories` contradicting `cve_count`, and reports the inflated score back to the caller — surviving until the next successful daily scan reaches that row (found 2026-07-24 discovery, scoring correctness lens) | When `security.scan_status === 'failed'` both routes apply the same guard as `bots/compute-scores.ts:163-175` (skip `cve_count`/`security_evidence`, retain the prior `score_security`, adjust `score_total`) and `refresh-score` reports the failure (e.g. `security_scan: 'failed'`) instead of silently returning the inflated score; the merge lives in one shared `lib/` helper used by bot and both routes, with a test asserting a `'failed'` result never lowers `cve_count` nor raises `score_security` relative to the stored row | open | no |
| S35 | P2 | `revalidate` is INERT on `/category/[category]` and `/servers` — both are fully uncached dynamic routes, and `/category` throws a 500 on any query error. `app/category/[category]/page.tsx:13` declares `revalidate = 604800` plus a `generateStaticParams` for all 22 categories, but `:59` awaits `searchParams` before every fetch, so the route appears in neither `routes` nor `dynamicRoutes` in `.next/prerender-manifest.json` (unlike `/best/[category]`, which is there) and prod returns `cache-control: private, no-cache, no-store` with `x-vercel-cache: MISS` (vs `PRERENDER` for `/best/ai-ml`). Per `node_modules/next/dist/docs/01-app/02-guides/caching-without-cache-components.md`, `fetchCache:'auto'` also does not cache fetches discovered after a request-time API, so the Supabase round trip is uncached too. Every crawler hit of the 22 sitemap-listed category pages and every `?sort=`/`?page=`/`?min_score=` permutation therefore runs a live anon query with zero cache absorption — measured 2.20s for `/category/other?sort=downloads&page=150` and 2.15s for `/category/ai-ml?q=agent&sort=name` against the 3s timeout — and `:120-123` THROWS on error, so one timeout is a 500 (this is the surface that 500'd during the S20 outage; S22 covers only `/analytics`). Secondary: `app/servers/page.tsx:130` awaits the `home_stats` RPC serially after the listing query instead of in parallel (found 2026-07-24 discovery, pages performance lens) | The category/servers listing data is wrapped in a tagged `unstable_cache` (as `app/page.tsx:95-99` and `app/security/page.tsx:92` already do) or `searchParams` is read only after the cached fetch so the declared `revalidate` applies — verified by `x-vercel-cache` no longer being MISS/no-store on a repeat request to `/category/ai-ml`; a transient query error on `/category/*` renders a degraded page instead of a 500; the `home_stats` RPC on `/servers` is issued in parallel with the listing query | open | no |
| S36 | P2 | The hosted `/mcp` endpoint collapses every user into ONE rate-limit bucket. `lib/mcp/api.ts:95-100` makes a public HTTPS round trip to `https://mcppedia.org/api/mcp` for every tool call (`MCPPEDIA_API_URL` is set nowhere in the repo, so `DEFAULT_BASE_URL` at `api.ts:1` always wins), forwarding only `Content-Type` and optionally `If-None-Match` — no `x-forwarded-for`. `app/api/mcp/route.ts:100-110` then keys the limiter on `getClientIp(request)` (`lib/rate-limit.ts:80-88`), which resolves to the Vercel egress IP of the lambda, never the MCP client's. One user's agent running 60 `get_server_details` calls in a minute makes the next unrelated user's single `search_servers` fail with "Rate limited. Try again in 60 seconds." (`lib/mcp/api.ts:120-123`), and the 60/60s budget is shared with direct `POST /api/mcp` callers who DO present real IPs — an abuse control structurally incapable of seeing the abuser (found 2026-07-24 discovery, MCP correctness lens) | The hosted MCP path either forwards a caller-derived identity that the limiter keys on, or skips the HTTP self-hop entirely (calls the shared query layer in-process) and rate-limits in `app/mcp/route.ts` on the real request; a test asserts two tool invocations from distinct client IPs produce distinct rate-limit keys and that 60 calls from one client do not exhaust another's budget. Any change to `lib/rate-limit.ts` touches a protected path | open | maybe |
| S37 | P2 | `/api/mcp` `action:"search"` applies `min_score` AFTER the RPC paginates — the S14 defect in an uncovered caller. `app/api/mcp/route.ts:148-156` calls `search_servers` with `page_size: limit` and does NOT pass `min_score_filter` (the param exists: `20260719120000_search_servers_filters.sql:17`), then filters in JS at `:179-181`. S14's acceptance criteria are scoped to `app/servers/page.tsx`, so this caller is not covered. `search_servers({query:"postgres", min_score:70, limit:5})` returns the 5 most RELEVANT matches, the JS filter empties the array, and `lib/mcp/tools.ts:87-88` reports `No servers found for "postgres".` while dozens of qualifying servers exist. Inconsistent too: the ilike FALLBACK branch does apply `min_score` in SQL (`:166`), so the same call succeeds or fails depending on whether FTS matched (found 2026-07-24 discovery, MCP correctness lens) | `/api/mcp` search passes `min_score_filter` (and category, for parity) into the RPC so filtering precedes `limit`, and the post-fetch JS filter at `:179-181` is removed; given a corpus where the top-`limit` relevance hits are all below `min_score` but at least `limit` higher-scoring matches exist, the action returns `limit` rows all with `score_total >= min_score`. Test lives in `__tests__/` (protected) | open | yes |
| S38 | P2 | `get_install_config`'s elicitation can never be answered on the stateless hosted endpoint, so the tool hangs until the function is killed. `lib/mcp/tools.ts:369-404` issues a server-to-client `elicitation/create` via `extra.sendRequest` when `client` is omitted, but `app/mcp/route.ts:42-46` builds a NEW `McpServer` plus transport per request with no `sessionIdGenerator` (stateless) — the pending-request map lives on that per-request `Protocol`, so the client's elicitation response (a separate `POST /mcp`) lands on a fresh instance with no matching handler and is dropped. `sendRequest` passes no `timeout`, so it waits `DEFAULT_REQUEST_TIMEOUT_MSEC = 60000` (`node_modules/@modelcontextprotocol/sdk/dist/esm/shared/protocol.js:8,712`), past the route's `maxDuration = 30`. Capability enforcement does not save it: `assertCapabilityForMethod` only runs when `enforceStrictCapabilities === true`, which is unset. Clients WITHOUT elicitation reject fast and work fine, so the feature is broken precisely for the clients it targets (found 2026-07-24 discovery, MCP correctness lens) | On the stateless HTTP transport the elicitation attempt is skipped (gated on advertised client capability or a stateless flag) or bounded by an explicit `timeout` well under `maxDuration` (e.g. 5s) so the handler always falls back to the `claude-desktop` default; a test asserts `get_install_config` with no `client` resolves in under 5s when no elicitation response arrives and returns a default config | open | no |
| S39 | P3 | `/api/mcp` writes an unvalidated, unbounded `action` string into `mcp_api_usage` BEFORE validating it. `app/api/mcp/route.ts:127-128` fires `increment_mcp_usage` (SECURITY DEFINER, RLS-bypassing, one row per distinct `(usage_date, action)` — `20260408010000_mcp_api_usage.sql:17-25`) ahead of the `switch (action)` that rejects unknown actions at `:361`; `action` is only checked for `typeof === 'string'` (`:120`) with no allow-list and no length cap. An unauthenticated attacker POSTing `{"action":"<random-uuid>"}` 60x/min/IP gets a 400 AND permanently inserts a junk row each time. Nothing prunes the table, and `app/analytics/page.tsx:539-544` reads it with `.order('usage_date', {ascending:true}).limit(600)` (comment: "~90 days * 6 actions") — once padded past 600 rows the MCP-usage panel freezes on the oldest window and silently omits all real recent usage. The RPC is also callable directly with the public anon key with no rate limit (found 2026-07-24 discovery, api-security lens) | The usage increment runs only for actions passing the allow-list (moved after validation or gated on a `KNOWN_ACTIONS` set), with unknown actions counted under a single fixed `'invalid'` bucket if at all; a test asserts an unknown or oversized `action` returns 400 and performs no `increment_mcp_usage` call; optional `p_action` length/allow-list check inside the RPC is noted as touching `supabase/migrations/**` | open | no |
| S40 | P3 | `bots/track-trending.ts` publishes a bogus `trending:N` and never clears stale tags. It computes `gain = repo.stargazers_count - server.github_stars` (`:29-36`) and calls it a WEEKLY star gain, but `bots/update-metadata.ts:112` overwrites `github_stars` daily and the crons are `update-metadata` at 06:00 UTC vs `track-trending` at 09:30 UTC — a 3.5-HOUR baseline. So `gain >= 10` is essentially unreachable, `trending:N` tags mostly stop being written, `getTrendingServers()` (`bots/generate-blog.ts:247-266`) returns `[]`, and the TRENDING article prompt ("gaining the most stars this week", `:571`) runs on empty or stale data; when a tag IS written, `N` is a 3.5-hour delta published as a weekly figure. Tags are also only cleared for servers the loop reaches and successfully fetches (`:52-53` `if (!repo) continue`, query capped at the top 500 by stars excluding `github_stars = 0`), so a server dropping out of the top 500 or whose repo 404s keeps `trending:45` indefinitely and `generate-blog` keeps citing it. And `tags` is read once at `:29-36` and written back over a multi-minute loop, silently reverting any concurrent `tags` write (found 2026-07-24 discovery, bots data-integrity lens) | Star gain is computed against a snapshot `update-metadata` does not overwrite (a `github_stars_7d_ago` column or a date-keyed `star_snapshots` row) so `trending:N` is a real 7-day delta; `trending:` tags older than the window are cleared for ALL servers, not just the reached-and-fetched top 500; the tag write is scoped to the `trending:` prefix rather than replacing the whole array from a stale read; tests assert the gain uses the 7-day baseline and that a server absent from the current run loses its stale tag | open | no |
| S41 | P3 | `bots/extract-schemas.ts` writes unvalidated model JSON into `servers.tools` and one bad response aborts the whole batch. `extractToolsWithHaiku` returns `JSON.parse(text)` with ZERO shape validation but is typed as the promised structure (`:66-82`). If the model returns only `{"tools": []}` — ordinary, given the prompt says "return empty arrays" — then `:158`'s `tools.length > 0 || resources.length > 0 || prompts.length > 0` hits `undefined.length`; there is no per-server try/catch, so it propagates to `main`, `run.fail()` marks the run failed, and the rest of the 200-row batch is dropped for the day. If it returns `{"tools": "search_docs"}`, `tools.length` is `9 > 0` so `update({tools, resources, prompts})` writes a STRING into the `tools` jsonb column, and downstream `bots/compute-scores.ts:93` casts it `as Tool[]` into `measureTokenEfficiency`/`scoreDocumentation` (`:196` calls `tools.filter`) while every renderer expects an array of objects. Related: `:161` always writes all three fields, so a row selected purely because `tools = '[]'` has any existing `resources`/`prompts` blanked to `[]` (found 2026-07-24 discovery, bots data-integrity lens) | Extractor output passes a zod schema (arrays of `{name: string, description: string, input_schema?: object}`) before any DB write, with a non-conforming response logged and skipped rather than written; each server is wrapped in a try/catch so one bad response cannot abort the batch; the update writes only the keys actually extracted, never blanking existing `resources`/`prompts`; tests feed `{}`, `{"tools":"x"}` and a valid payload asserting no write, no write, and one validated write | open | no |
| S42 | P3 | A SECOND, materially different scoring engine still lives in the DB and is RPC-called by a maintenance script, silently rewriting the same `score_*` columns. `supabase/migrations/20260426120000_fix_scoring_formula.sql:19-26` computes Security as `30 - 10*cve_count - 4(no auth) - 3(no license) - 8(archived) + 5(security_verified)` — no tool-poisoning, injection-vector, tool-safety, tool-stability or dependency-health term, i.e. it ignores 12 of the TS engine's 30 points and subtracts from a ceiling instead of summing evidence (`lib/scoring.ts:753-767`); Documentation is likewise `+3 for "has any tools"`. `scripts/apply-classifications.ts:197,282` RPCs it for every touched row, so running that script rewrites a tool-poisoned server (`score_security ~= 21`, `has_tool_poisoning = true`) to `score_security = 30` while leaving `has_tool_poisoning`/`tool_poisoning_flags`/`security_evidence` intact — the ScorePanel then shows a poisoning warning next to a perfect 30/30 bar and the server jumps in every `score_total DESC` listing. The stale `compute_all_scores()` (`20260402010000_scores_security_registry.sql:212-223`) can do this fleet-wide in one call (found 2026-07-24 discovery, scoring correctness lens) | Exactly one implementation writes the `score_*` columns: either `compute_server_score`/`compute_all_scores` are dropped or reduced to a raising no-op and `scripts/apply-classifications.ts` re-scores through the TS engine, or the SQL is proven equivalent to `lib/scoring.ts` by a fixture test; `grep -r "compute_server_score" scripts/ bots/ app/` returns no callers and `docs/org-memory/codebase.md` records the SQL formula as dead. Touches `supabase/migrations/**` | open | yes |
| S43 | P3 | Two derived security columns are wrong for the "no data" case and one is rendered against the wrong denominator. (a) `dangerous_pattern_count` is computed as `max_points - points` of the `tool-safety` evidence in all three writers (`bots/compute-scores.ts:180-182`, `app/api/server/[slug]/refresh-score/route.ts:169-171`, `app/api/submit/route.ts:246-248`), but a server with zero extracted tools gets `{pass: null, detail: 'No tools to analyze', points: 0, max_points: 3}` (`lib/scoring.ts:269-270`) — so `dangerous_pattern_count = 3`, and `lib/mcp/tools.ts:150` prints "Dangerous patterns: 3" for a server whose own evidence block on the same page reads "No tools to analyze" (the field is in the public MCP field list, `app/api/mcp/route.ts:37`). (b) `dep_health_score` is the `dep-health` evidence `points`, range 0-3 (`lib/scoring.ts:591-652`), but `lib/mcp/tools.ts:151` renders it as `${s.dep_health_score}/100` — every package on deps.dev is reported to LLM consumers as "Dep health: 1/100" (found 2026-07-24 discovery, scoring correctness lens) | `dangerous_pattern_count` is null (or 0) when the `tool-safety` check could not run (`pass === null`), consistently in all three writers; `lib/mcp/tools.ts` renders `dep_health_score` against its real `/3` range (or the value is normalized at write time and the denominator matches); tests assert the derived-count helper returns null for a no-tools server and 0 for one clean tool, and that the MCP security report contains no `/100` for `dep_health_score` | open | no |
| S44 | P3 | Fractional `min_score`/`limit` are accepted by the MCP tool schemas and then silently DISCARDED. `lib/mcp/tools.ts:68`, `:69`, `:495`, `:595` use `z.number()` without `.int()`, so the JSON Schema is `{"type":"number"}` and a model may legitimately send `62.5`; the handler's `safeInt` (`app/api/mcp/route.ts:69-73`) returns the FALLBACK for any non-integer (`if (!Number.isFinite(n) || !Number.isInteger(n)) return fallback`). `search_servers({query:"email", min_score:62.5})` becomes `safeInt(62.5, 0, 0, 100)` = `0` = `undefined`, dropping the score floor entirely and returning servers scoring 4/100 as if they satisfied the request. Same class: `get_trending({limit:12.5})` silently returns 10 and `what_changed({limit:25.5})` silently returns 20 while the text header implies the request was honored (found 2026-07-24 discovery, MCP correctness lens) | The four numeric tool params are `z.number().int()` so a non-integer is rejected with a validation error the model can correct, AND `safeInt` rounds rather than falling back for finite non-integers; a test asserts `min_score: 62.5` either errors or filters at 62/63 and never returns rows below the floor | open | no |
| S45 | P3 | `get_install_config` reports a target client whose config it does not have. `lib/mcp/tools.ts:416-426` resolves `configs[targetClient] || configs["default"] || configs[Object.keys(configs)[0]]` and then unconditionally prints `Target client: ${targetClient}` (`:424`) — but the only writer of `install_configs` keys it exclusively `'claude-desktop'` (`bots/extract-install-info.ts:198,200,211,222,238,249`), and `compatible_clients` is fetched (`app/api/mcp/route.ts:43-46`) but never read by the tool. `get_install_config({slug, client:"windsurf"})` therefore emits Claude-Desktop-shaped JSON under the heading `Target client: windsurf`, with no indication the config is not windsurf-specific and no check that the server even lists windsurf as compatible — an agent writing that block into a windsurf config file has been told, falsely, that it is windsurf config (found 2026-07-24 discovery, MCP correctness lens) | When no config exists for the requested client the response states which client's config is actually being shown (or synthesizes a client-appropriate one) and surfaces `compatible_clients`; a test with `install_configs = {"claude-desktop": {...}}` and `client: "windsurf"` asserts the output discloses the substitution rather than printing an unqualified `Target client: windsurf` | open | no |
| M3 | P2 | `mcppedia-server/` is NOT in this repo — `.gitignore:46-47` excludes it as a "separate repo: BbekShr/mcp-server-mcppedia" and `git ls-files` returns zero matches — yet CLAUDE.md §3 lists it as part of the architecture. Its `src/{api,format,prompts,resources,tools}.ts` are byte-identical to `lib/mcp/*` except for `.js` import suffixes, so every fix to `lib/mcp/` (S36-S38, S44, S45) silently does NOT reach the published standalone/stdio server, and agents reason about a directory they cannot see or change (found 2026-07-24 discovery, MCP correctness lens) | A decision is recorded in `docs/org-memory/codebase.md`: either extract the shared MCP query/format layer into a package both repos consume, or document the manual sync procedure (which files, when, how verified) and note in CLAUDE.md §3 that the directory is a gitignored sibling checkout; whichever is chosen, a fix to `lib/mcp/**` has an explicit, written path to the standalone server. CLAUDE.md is protected | open | yes |
| M4 | P3 | There is NO unique index on `servers.github_url`, `npm_package`, or `pip_package` — only on `slug` (`supabase/migrations/20260402000000_initial_schema.sql:6`) — despite `bots/sync-registry.ts:229-232` asserting dedup happens "with the dedup indexes". Duplicate-listing prevention therefore rests entirely on in-memory sets built by the four error-swallowing offset loops in `bots/discover.ts:45-112` (see S24): one swallowed page error and a re-discovered repo becomes a second row, which `detect-duplicates` must then find and archive (see S32) (found 2026-07-24 discovery, bots data-integrity lens) | A decision is recorded on whether to add unique (or partial-unique, NULLs excluded) indexes on `github_url`/`npm_package`/`pip_package` after auditing existing duplicates, or to fix the misleading `sync-registry.ts:229-232` comment and rely on the hardened S24 loops; either way the repo no longer claims indexes that do not exist. Touches `supabase/migrations/**` if indexes are added | open | maybe |
| M5 | P3 | `/improve-cycle discover fix` structurally cannot complete in one session — the skill says "run discovery as above, wait for the records-only PR to be merged by the human, pull main, then run phases 2–8 per finding", but the same skill forbids a cycle from ever merging, so the mode always stalls at the wait. Observed on the 2026-07-24 discovery run | The `discover fix` step is rewritten to a procedure a cycle can actually execute end-to-end without merging — e.g. branch the fix PRs off `main` (not off the unmerged records branch) so finding and fixing stay in separate diffs while both sit open for the human, or the mode is documented as explicitly two-session. `.claude/skills/improve-cycle/SKILL.md` is not on the CLAUDE.md §5 protected list, so a cycle may edit it | open | no |
| S46 | P3 | `has_injection_risk` is computed differently by the bot than by the two API routes, so the same server gets a different flag depending on which writer touched it last. `bots/compute-scores.ts` derives it from the `injection` evidence entry alone, while `app/api/server/[slug]/refresh-score/route.ts` and `app/api/submit/route.ts` derive it from `injection` OR `tool-poisoning`. A server submitted (or score-refreshed) with a poisoned tool description gets `has_injection_risk: true`, and the next nightly `compute-scores` run silently flips it to `false` — or the reverse, depending on order. The flag drives UI badges and is in the public MCP field list (found 2026-07-24 while fixing S25/S34; pre-existing on `main`, deliberately not folded into that fix) | The three writers derive `has_injection_risk` from one shared expression (ideally the same `lib/` helper introduced for the OSV-failure merge), so the flag is stable regardless of which writer ran last; a human decision is recorded on whether tool-poisoning should count as injection risk, and the chosen semantics are applied in all three places; a test pins the derivation | open | no |
| S47 | P3 | Four `fetchAllRows` callers pass NO `.order()` at all, violating the helper's own documented contract — `bots/lib/supabase.ts` paginates by offset and its doc comment explicitly says "Build the query (filters, ordering) without `.range()`", i.e. ordering is the caller's job, and without a unique tiebreak its pages can skip and duplicate rows. Offenders: `bots/sync-registry.ts:87,94`, `bots/check-broken-links.ts:62`, `bots/enrich-descriptions.ts:78`, `bots/extract-install-info.ts:135`. Consequences vary by caller and were not individually assessed; `sync-registry` is the concerning one since a skipped row there interacts with the missing unique indexes (M4). Note `bots/detect-duplicates.ts:86` is the same class but already filed as S32, and `bots/discover.ts` was fixed under S24 (found 2026-07-24 while fixing S24) | Every `fetchAllRows` call site supplies a unique `.order()` tiebreak (`id`, or a composite ending in `id`), verified by reading all call sites; the helper's doc comment is strengthened to state that a unique tiebreak is REQUIRED, not optional; a follow-up decision is recorded on whether `bots/discover.ts`'s four hand-rolled offset loops should migrate onto `fetchAllRows`, which would also give them its 4-attempt backoff that a raw loop lacks | open | no |
| M6 | P3 | `npm test` at the repo root is unreliable during parallel-worktree work — `vitest.config.ts` sets no `exclude`, so vitest's default crawl picks up every copy of the 10 tracked test files inside agent git worktrees under `.claude/worktrees/`. Observed 2026-07-25 during the batch-1 cycle: `npm test` reported `50 test files / 600 tests, 4 failed` instead of `10 / 120`, with the failures in a worktree's copy of `components/home/__tests__/Hero.test.tsx` (duplicate module resolution — the same file passes both on `main` and inside the worktree itself). Harmless to CI, which clones fresh, but it means the org's own verification bar produces a red, misleading result in exactly the parallel-worktree workflow `/improve-cycle N` and discovery batches use — and a future cycle could read that red as a real regression or, worse, read a real failure as noise (found 2026-07-25 while gating batch 1) | `vitest.config.ts` excludes `.claude/worktrees/**` (and any other scratch checkout dir) from test discovery, so `npm test` at the repo root reports 10 files / 120 tests whether or not worktrees are present; no test is skipped and no assertion weakened — only non-source scratch checkouts are removed from the crawl; verified by running the gate with worktrees present and absent | open | no |
| S48 | P1 | Auto-approve is broken for EVERYONE, and the schema of record says so — `app/api/edit/route.ts:59-73` inserts an `edits` row with `status: 'approved'` through the **authed** server client (`createClient()` at `:15`; the admin client at `:92` is used only for the later `servers` write), but the `edits` INSERT policy pins `status = 'pending'` (`20260610000000_security_hardening.sql:28-36`). So if that migration is applied in prod, every low-risk edit from a trusted editor/maintainer/admin is RLS-denied and the route returns its generic 500 at `:78-80` instead of applying the edit. This also gates S23's worst outcome: the escalation's final step ("writes straight to `servers` across 39k listings") requires `20260610000000` NOT to be applied, though the escalation itself is real either way and the gate opens the instant anyone "fixes" the 500 by moving that insert to the admin client. Because there is no deploy-time migration runner (M2), which migrations are actually live in prod CANNOT be verified from an agent session (found 2026-07-25 while verifying the S23 backfill sources) | A human with prod access checks `pg_policies` and reports whether `20260610000000` is applied, and that fact is recorded in `docs/org-memory/codebase.md`; the auto-approve insert then either goes through the service-role admin client (matching how the subsequent `servers` write is already done) or the INSERT policy is widened to permit `status='approved'` for privileged roles — decision recorded; the RLS error is checked rather than surfacing as a generic 500; a test covers a trusted user's low-risk edit applying end-to-end | open | maybe |
| S49 | P2 | `bots/detect-duplicates.ts:34-72` `reparent()` swallows every failure and the caller archives regardless, orphaning user data on an invisible row. Each `.update()` error is only `console.warn`'d (`:40`, `:69`), and the two `.select()` calls at `:46` and `:52` do not destructure `error` at all — so a failed keeper-rows lookup yields `keeperRows === null`, `keeperUsers` becomes empty, and the bot then attempts to move rows that DO conflict, failing the unique constraint and again only warning. The caller at `:141-148` awaits `reparent` and unconditionally proceeds to `update({ is_archived: true })`, checking only the archive write's error. Realistic outcome: reviews, discussions and favorites stay pointed at the dupe, the dupe is archived anyway, and that user data is orphaned on a row no longer visible anywhere, while the run reports success and `run.addUpdated()` counts it (found 2026-07-25 while fixing S32; deliberately not folded into that PR because it needs a policy decision, not a one-line guard) | `reparent` returns a per-table success/failure status instead of `void` and checks every `.select()`/`.update()` error; a recorded decision on the failure policy (abort the archive and retry next run, vs. archive anyway and record the orphan) is implemented; the outcome is reflected in `run.setSummary` so a partial reparent is visible rather than reported as success; a test covers a reparent failure not resulting in a silent archive | open | no |
| M7 | P2 | NOTHING in the verification bar parses, let alone executes, `supabase/migrations/**` — `npx tsc --noEmit`, `npm run lint`, `npm test` and `npm run build` never touch SQL, so a migration can be syntactically invalid, semantically wrong, or leave a stale duplicate object behind and still ship past a fully green bar. Two P1 schema bugs have now done exactly that: S21 (duplicate `search_servers` overload, caused a live 500 on `/api/search`) and S23 (duplicate `profiles` UPDATE policy, a live privilege escalation) — both the same root cause, a `CREATE` that did not supersede a renamed object. The S23 migration's forensics snapshot and four backfills are reviewed-by-eye only. Compounding it, M2 means migrations do not auto-apply, so prod schema state cannot be verified from a session either (found 2026-07-25 while shipping S23) | A migration gate exists and runs in CI: at minimum every file in `supabase/migrations/**` is applied in order against a scratch Postgres (Supabase CLI local stack or a plain `postgres:16` service container) and the run must succeed; ideally the same job then asserts schema invariants that would have caught both incidents — e.g. no duplicate function overloads and no more than one permissive policy per (table, command, role). Touches `.github/workflows/**` (protected) | open | yes |
| S50 | P1 | `main`'s production build is currently surviving on RETRIES — a control `npm run build` against real prod env on `main` emitted **7** `Failed to build … because it took more than 60 seconds. Retrying again shortly.` lines. Next retries each route 3× before failing the whole build, so `main` is one slow query away from a blocked deploy, which is exactly how PR #75's first attempt failed. Routes over budget: `/analytics/page` (needed multiple attempts) and `/sitemap-servers-1.xml` + `-2.xml` (over on attempt 1). Neither the env-less CI build (fetches nothing) nor a warm local connection reproduces this — only a real-env build does, and there is no gate that runs one. PR #75 (S11 keyset pagination) removes the sitemap half; the `/analytics` half is unaddressed — S22 covers only its throw-vs-degrade behavior, not its build-time cost (found 2026-07-25 while gating S35) | `npm run build` against real prod env on `main` emits ZERO `Failed to build … 60 seconds` retry lines — i.e. every prerendered route completes inside the budget on its first attempt; `/analytics`'s build-time fetch cost is reduced (or the route stops prerendering at build) so it no longer needs retries; a decision is recorded on whether a periodic real-env build check should exist, since neither CI nor a local build catches this class (relates to M7 and S3) | open — ESCALATED 2026-07-25 07:00Z: this is no longer a fragility, it is a LIVE OUTAGE. Production deploys off `main` are now FAILING, not merely retrying. Evidence: PR #83's Vercel build (dpl_Fk4fTG8G8iKX7vzGtnzdTAfCBJmf) shows `/sitemap-servers-1.xml` exhausting `after 3 attempts` → `Export encountered an error … exiting the build` → `Command "npm run build" exited with 1`. The same failure hit PRs #79, #80, #81, #82, #83 — every branch based on `main` without the sitemap fix. The natural experiment is clean: PR #75 (S11 keyset pagination, which REPLACES those three routes) deploys SUCCESSFULLY, while same-hour builds of unfixed branches fail. **Merging PR #75 unblocks deploys; PR #84 removes the `/analytics` half of the load.** Until one of them merges, nothing can ship to production — RESOLVED 2026-07-25 (PR #86, plus follow-up): all 13 mergeable PRs were merged; the sitemap half went away with #75 as predicted, but the `/analytics` half did NOT — #84 removed the throw and the servers-walk yet left 12 parallel `count: 'exact'` queries that measure ~4.5s warm and ~80s COLD, so `main` still hard-failed `Export encountered an error on /analytics/page` on the first build after merging. A slow SUCCESS is invisible to the `.catch` S22 added, which is why that work did not prevent it. Fixed by bounding the batch with a new `withDeadline` (`lib/retry.ts`) and degrading to the existing "temporarily unavailable" notice. Vercel then failed on the NEXT route over budget, `/llms-full.txt`, whose root cause was different and worse: `.order('score_total', {nullsFirst: false})` asks for `DESC NULLS LAST`, which `servers_score_active_idx (score_total DESC)` cannot satisfy, forcing a full sort of the table — 15s versus **172ms** for the identical 150 rows, the same trap `lib/sitemap-shared.ts:229-237` already documents. Production deploy is green and both routes serve 200. Residual, NOT fixed here and filed as S53/S54: the `/analytics` histogram still degrades to the notice during a build even at a 30s budget (build-time worker contention), and one `/sitemap-servers/2` 60s retry remains, so the zero-retry acceptance criterion above is still unmet | no |
| S51 | P2 | The advisory upsert is duplicated in FOUR places and the two API copies still have S33's append-only bug. `bots/compute-scores.ts` now reconciles (S33, PR #80), but `app/api/server/[slug]/refresh-score/route.ts:188-209` and `app/api/submit/route.ts:265-285` are byte-identical copies of the bot's OLD block — so an on-demand score refresh or a new submission still writes advisories that are never closed, re-creating the orphaned-open-CVE state S33 fixed for the nightly path. Left out of PR #80 deliberately: both copies collide with PR #73's region, and the right endgame is folding all three onto one shared helper once that lands (found 2026-07-25 while fixing S33) | The three advisory writers share ONE helper (natural home: alongside `lib/score-merge.ts` from PR #73, or `lib/security-columns.ts` from PR #83) that both upserts and reconciles, with the same `!osvFailed` guard the bot uses; a stale advisory is closed regardless of which writer ran; a test covers reconciliation via the refresh-score path | open | no |
| S52 | P2 | `app/api/server/[slug]/refresh-score/route.ts:78` writes `is_archived: repoMeta.archived` UNCONDITIONALLY through `createAdminClient('refresh-score')`, with no duplicate check — so a maintainer opening a merged duplicate and clicking "refresh score" instantly resurrects it (the keeper's repo is alive, so `repoMeta.archived` is `false`). The listing re-enters search, sitemaps and category pages while `/s/<slug>` still 301-redirects to the keeper (`next.config.ts` holds 15 such redirects), i.e. an indexed listing whose detail page redirects away. Compounding it, the write lands in `server_changes` as `(false, 'refresh-score', actor_id NULL)`, rewriting the very `is_archived` audit history any un-archive gate would read to decide WHY a row was archived — so this path both bypasses and corrupts the S9 gate (found 2026-07-25 by the data-integrity review of the S9 attempt; pre-existing, not introduced by that work) | `refresh-score` is archive-forward only — it may set `is_archived: true` when GitHub reports the repo archived, but never clears it (e.g. `is_archived: repoMeta.archived ? true : server.is_archived`), matching the invariant `bots/update-metadata.ts` already follows; a test covers refresh-score on an archived row leaving `is_archived` unchanged | open | no |
| S53 | P2 | The `/analytics` 12-month additions histogram still degrades to "Monthly breakdown temporarily unavailable." on every production build, because no index supports its shape. The 12 queries are `count: 'exact'` over `WHERE is_archived = false AND created_at >= a AND created_at < b`; the only candidate index is `servers_created_idx on servers(created_at desc)` (`20260402000000_initial_schema.sql:87`), which does not include `is_archived`, so each count range-seeks and then heap-fetches every row in the month to test the flag. Measured against prod (`servers` = 63,761 rows, 41,148 non-archived): ~7-13s standalone, ~80s on a cold cache. PR #86 bounds the batch at 30s so it can no longer fail the deploy, but during a build three Vercel export workers contend over the same Postgres while generating ~2k pages and the batch loses the race — verified: a local build at a 30s budget still emitted the degraded notice into `.next/server/app/analytics.html`. Because `revalidate = 86400`, users see the chart missing for up to 24h after each deploy, and deploys are frequent. Runtime (uncontended) regeneration does succeed, so the data is fine — only the build-time path degrades (found 2026-07-25 while fixing S50) | A partial index `ON servers (created_at) WHERE is_archived = false` (or an RPC returning all 12 monthly counts in ONE round trip via `GROUP BY date_trunc('month', created_at)`, which also removes 11 of the 12 round trips) makes the batch fast enough to survive a contended build; a real-env `npm run build` shows `.next/server/app/analytics.html` containing rendered bars rather than the degraded notice; the 30s deadline stays as a backstop rather than being the thing that saves the build. Touches `supabase/migrations/**` (protected) | open | yes |
| S54 | P2 | The `NULLS LAST` index-defeat that broke the `/llms-full.txt` deploy (S50) is a REPEATED pattern, and the remaining call sites were not audited as part of that fix. `.order(col, {ascending: false, nullsFirst: false})` emits `DESC NULLS LAST`, which a plain `(col DESC)` btree cannot satisfy — PostgreSQL's DESC default is NULLS FIRST — so the planner falls back to sorting the whole table. Confirmed cost on the one measured case: 15s vs 172ms for the same 150 rows, an 87x difference, and the `nullsFirst: false` was pointless there because ZERO non-archived rows have a null `score_total`. Un-audited remaining sites: `app/servers/page.tsx:136` and `app/category/[category]/page.tsx:123` (both `github_last_commit`, which has NO index at all, so these sort 41k rows on a user-facing listing sort — plausibly related to the S27/#65 statement-timeout incident), `app/page.tsx:137` (`published_at`), `bots/detect-duplicates.ts:116` (`data_quality`), `scripts/dump-servers.ts:111-112,149`. `lib/sitemap-shared.ts:253` is a deliberate documented fallback and is fine. Note the general remedy is `.not(col, 'is', null)` + default ordering, which is only equivalent when the null tail is genuinely absent or swept separately — each site needs that checked, not a blind rewrite (found 2026-07-25 while fixing S50) | Every `nullsFirst: false` call site is audited with a measurement, not by eye: each is either shown to be cheap (small table, or an index that satisfies the ordering), rewritten to the index-friendly form with equivalence verified against the old result set, or given the index it needs; the `github_last_commit` listing sorts specifically are confirmed not to be timing out against real prod data; the trap is written into `docs/org-memory/codebase.md` so it stops being rediscovered per-incident | open | maybe |
| S55 | P2 | `bots/extract-schemas.ts`'s extraction queue can starve permanently AND burns its full LLM/API budget doing nothing. `:160-165` selects `github_url IS NOT NULL` ∧ `tools = '[]'` with `.limit(200)` and **no `.order()`** — the only `.limit()` in `bots/` without one (all 15 others are ordered; `freshness-probe.ts:25` is provably single-row) — so the daily 08:00 UTC cron (`.github/workflows/extract-schemas.yml:4`) re-reads the same seq-scan prefix every run. The only DB write is `:205-218`, gated on a NON-EMPTY extraction (`:201-203`), so no attempt is ever recorded: rows that can never succeed (no README `:188-191`, unparseable URL `:183`, bad model shape `:194`, or a README that genuinely documents no tools) are never displaced and are re-processed daily at one Haiku call + one GitHub README fetch + a 500ms sleep (`:225`) each. Once 200 unextractable rows occupy the head, the bot does ZERO useful work forever and never turns red. `tools = '[]'` therefore conflates "never attempted", "attempted and failed" and "genuinely has no tools", and `run.setSummary` (`:230`) has no bucket to tell them apart. Also `:160` does not destructure `error`, so a read failure yields `servers = null` → logs "No servers need schema extraction" → `run.finish()` reports SUCCESS. Cost of an empty `tools`: **29-39 of 100 points**, not the 31 quoted in the issue — efficiency a flat 20 (`lib/scoring.ts:806-816`), security only 9-11 of 11 because `scanSecurity` clamps a 37-point max to 30 (`:766-768`), plus up to 8 withheld inside `scoreDocumentation`'s `if (tools.length > 0)` (`:904-921`) and 2 from `inferAuthFromTools` (`:670-680`); `checkToolDefinitionStability` withholds nothing (hash of `[]` is deterministic, `:508-517`). Reads to visitors as a security/efficiency verdict rather than as missing data (GitHub issue #70, `net.convertica/mcp` at 56/C) | The candidate query has a deterministic ordering AND every terminal outcome records an attempt, so an unextractable server is displaced from the queue head on the next run instead of re-processed daily; a run over ≥400 unextractable servers reaches the second 200 on its second run (test with a fake client asserting the two runs' selected ids are disjoint); `run.setSummary` distinguishes no-README / zero-tools-extracted / bad-model-shape from `failed`; the page `error` is destructured and a read failure FAILS the run. **Human decision required first:** reuse `updated_at` as the attempt stamp (index-friendly and migration-free — `.order('updated_at', {ascending: true})` is the reverse scan of `servers_updated_idx`, so S54-safe; but `updated_at` feeds sitemap `lastModified` at `lib/sitemap-shared.ts:212,290`, so touching ~200 rows/day churns `lastmod` on unchanged pages and burns crawl budget) vs. add a dedicated `tools_extraction_attempted_at` column (clean, but a protected migration + M2 means it will not auto-apply). Do NOT ship `.order('id')` alone — it converts today's incidental determinism into guaranteed permanent starvation. Touches `lib/__tests__/**` | open | yes |
| S56 | P2 | A signed-in user can be permanently shown "Sign in to edit this page" on `/s/[slug]/edit` while the nav simultaneously shows their profile, My Servers and Sign out — reported by a server owner who could therefore not correct their own listing (GitHub issue #68, second half). NOT an auth-layer defect: `proxy.ts`'s cookie gate does cover `/s/*/edit` (`:28-41` excludes only static/sitemap paths), the project ref matches, and the edit page performs ZERO server-side session reads, so the middleware cannot be implicated. Root cause is `app/s/[slug]/edit/page.tsx:139,152-154`: a `'use client'` page whose single `supabase.auth.getUser()` **discards the `error` half and has no `.catch`**, with `user` initialised to `null` and NO loading flag — so `null` means both "not resolved yet" and "signed out", and `:307-315` renders the sign-in branch. `getUser()` is a NETWORK call to `/auth/v1/user` that converts any `AuthError` (GoTrue 429/5xx, transient fetch failure) into `{user: null, error}`, and can additionally **reject** on a Web-Locks acquire timeout (>5s, e.g. a second tab with a stalled refresh) because `NavigatorLockAcquireTimeoutError` is not an `AuthError` and is re-thrown — an unhandled rejection that leaves the component in its initial state. There is no `onAuthStateChange` subscription, so nothing ever corrects it without a manual reload, and clicking the page's own sign-in link (`:312`) completes OAuth and returns to the same race. `components/Nav.tsx` survives the identical failure only because it pairs `getUser()` with `onAuthStateChange` (`:22,35`), whose `INITIAL_SESSION` event is served from the cookie with no network call and lands second — which is precisely why the nav and the page disagree. Guaranteed sub-case with no failure needed: the server-rendered HTML of `/s/[slug]/edit` contains "Sign in to edit this page." for EVERY visitor until hydration + the round trip completes, so anyone clicking during that window is sent through OAuth needlessly. Repo-wide pattern: `app/submit/page.tsx:38`, `app/welcome/page.tsx:34`, `app/admin/page.tsx:194`, `components/FavoriteButton.tsx:18`, `components/ReviewSection.tsx:86`, `components/DiscussionSection.tsx:48` are all bare-`getUser()` sites with the same two failure modes | The edit page distinguishes three states — unresolved (renders a loading state, NEVER the sign-in copy), authenticated, and confirmed-signed-out — and derives auth from a session that does not require a successful `/auth/v1/user` round trip, subscribing to `onAuthStateChange` (as `components/Nav.tsx:35-41` does) so a later `SIGNED_IN`/`TOKEN_REFRESHED` updates the view without a reload; the auth call's `error` AND its rejection both surface as a retryable "couldn't verify your session", never as "Sign in to edit this page"; tests render the page with a mocked client whose `getUser` resolves `{user: null, error: AuthRetryableFetchError}` (and, separately, rejects) while the session is valid, and assert the editor renders with no sign-in copy, plus a case asserting the pre-resolution render contains no sign-in copy. Fix the same pattern in the six sibling call sites or file a follow-up row for them. Touches `__tests__/**` | open | yes |
| S57 | P2 | Hosted **remote** MCP servers are described to the world as unauthenticated local clones, in prose that also ships as `FAQPage` JSON-LD to Google. Two independent halves. (a) **The pipeline throws the remote facts away at parse time.** `bots/sync-registry.ts:30-33` types `remotes` as `{transport, url}` — there is NO field for the registry's declared secret/required headers — and `:130` keeps only `rs.remotes?.flatMap(r => r.transport)`, **discarding `remotes[].url` entirely**; no `remote_url` column exists in any migration, so the endpoint a hosted server is actually reached at is never stored even though the registry publishes it. The insert (`:207-231`) never sets `has_authentication` or `requires_api_key` at all, so every registry import starts life as "no auth" regardless of what its registry record declares. (b) **The UI renders that missing data as positive assertions, ignoring `transport`.** `components/ServerFAQ.tsx:12-18` turns falsy `has_authentication` into "It does not require authentication, so any local process can connect" — a confident security claim manufactured from absent data, and flatly wrong for an authenticated HTTPS endpoint; `:41` falls back to "can be installed by cloning its GitHub repository and following the setup instructions in the README" for any server without npm/pip/`install_configs`; and `components/server/InstallMatrix.tsx:122-125` / `:106` emit a stdio stub (`command: '<see-readme>', args: []`, `claude mcp add … -- <command> <args>`). `buildServerFAQs` never reads `server.transport`, so a `streamable-http` server gets the full local-stdio treatment. Both FAQ strings are fed to `generateFAQJsonLd` (`lib/seo.tsx:219-232`) at `app/s/[slug]/page.tsx:163,195`, so the false "does not require authentication" line is emitted as structured data for search engines, not just on-page text. Reported by the `datamcp` publisher (GitHub issue #68, first half) — same missing-data-as-verdict class as S55. **Do not hand-edit the reported row**: the reporter's supplied "canonical metadata" is a lead to verify against the registry API and the repo, not ground truth (CLAUDE.md §4) | `sync-registry` persists the remote endpoint URL and the registry-declared auth requirement (`remotes[].headers`/required-secret shape verified against a live registry record, not assumed) so `has_authentication`/`requires_api_key` reflect the registry rather than defaulting false; `buildServerFAQs` and `InstallMatrix`'s fallback branch on `transport` — a remote/`streamable-http` server gets a URL-based install (or an explicit "configure the hosted endpoint" instruction), never a clone-the-repo or `<see-readme>` stdio stub; and absent auth data renders as "authentication requirements unknown", never as "does not require authentication". Tests cover a remote-transport server with declared auth and a remote server with NO auth data, asserting neither yields the clone/`<see-readme>`/no-auth copy in the FAQ or the JSON-LD. Storing the endpoint URL needs a new column (protected migration; see M2/M7) — the human decides whether to add it or to derive install text from `transport` alone in a first pass. Touches `supabase/migrations/**` and `__tests__/**` | open | yes |
| S58 | P1 | **`bots/sync-registry.ts` parses a registry schema that no longer exists — FOUR of its five field mappings are dead**, so every registry import lands stripped of its package, its identity, and its transport. Verified 2026-07-25 against the exact endpoint the bot polls (`REGISTRY_API = .../v0.1`, `:17`) with `?version=latest`; live records declare `$schema: .../schemas/2025-12-11/server.schema.json`. (a) `RegistryServer.id` (`:20`) **does not exist** — the entry is `{server, _meta}` and the server object has NO `id` field (confirmed on both `/v0` and `/v0.1`), so `rs.id` is `undefined` everywhere it is used (`:133,138,156-159,175,228`). Therefore `existingIds.has(rs.id)` is always false and the "already synced → bump `registry_synced_at`" fast path (`:133-141`) is DEAD CODE; `registry_id` is written as `undefined` in both the update (`:157`) and the insert (`:228`), so the column stays NULL for every row, so `getExistingRegistryIds()` (`:88-91`, filtered `.not('registry_id','is',null)`) returns an EMPTY set forever — the failure is self-reinforcing and every nightly run re-evaluates the entire registry from scratch against `github_url` alone. (b) `packages[].registry_name`/`.name` (`:26-27`) are now `registryType`/`identifier`, so `find(p => p.registry_name === 'npm')` and the `'pypi'` twin (`:128-129`) can NEVER match: **`npm_package` and `pip_package` are always null on every registry import**. That is the upstream cause of S57's `<see-readme>`/clone-the-repo fallback, and it silently costs score — `checkCVEs` (15) and dependency-health (3) are package-gated in `scanSecurity`. (c) `remotes[].transport: string[]` (`:31`) is now `type: string`, so `rs.remotes?.flatMap(r => r.transport)` (`:130`) yields `[undefined]` for every remote server rather than a transport list; the `\|\| ['stdio']` fallback only fires when `remotes` is absent entirely. (d) `version_detail?.version` (`:24`) is now a flat `version`. Additionally `_meta['io.modelcontextprotocol.registry/official'].status` and `isLatest` are never read at all, so a `deleted`/`deprecated` registry record is ingested as active. Reported symptom (GitHub issue #25): `io.github.kaitoInfra/twitterapi-io-mcp-server` is missing from MCPpedia — the record IS present, `status: "active"`, `isLatest: true` since 2026-05-23, on the exact URL the bot polls, with `packages[0].identifier = "@kaitoinfra/twitterapi-io-mcp-server"`. The reporter's dedup-collision guess is NOT the cause: that slug-swallowing bug was fixed in `dfc0beb` (whose comment at `:170-177` names issue #25) | `RegistryServer` matches the live `2025-12-11` schema — entry unwrapping, `packages[].registryType`/`identifier`, `remotes[].type`/`url`/`headers`, flat `version`, and a stable identity key (the registry `name`, since there is no `id`) — with a fixture captured from the live API committed under test so the next upstream rename fails a test instead of silently zeroing a field; `registry_id` (or its replacement) is actually persisted, restoring the already-synced fast path; `_meta…/official.status` is honored so non-active records are not ingested as active; and the bot FAILS (or at minimum reports red via `run.setSummary`) when a run maps 0 npm/pip packages across the whole registry, since that is the signature of exactly this drift. Verify against the reported server specifically: after a sync it resolves with its npm package populated. Note the fix does not repair rows already imported with null packages — state whether a backfill is in scope (bounded cost) or a follow-up row. Also satisfies standing goal 2 (keep-current). Touches `__tests__/**` | open | yes |
| M8 | P3 | `npm run lint` — the documented gate command (CLAUDE.md §2) — is not reproducible in a working tree that contains untracked non-app files, and there is no `.gitignore`-awareness to protect it. `eslint.config.mjs` uses flat config, which does NOT read `.gitignore` (that needs `includeIgnoreFile` from `@eslint/compat`), so eslint lints every matching file on disk regardless of tracking. Observed 2026-07-25: three `.blogdata*.tmp.ts` local analysis scratch scripts (read-only Supabase queries, no credentials) sitting untracked in the repo root produced 5 `no-explicit-any` errors and a red `npm run lint`, while `npx eslint $(git ls-files '*.ts' '*.tsx')` — what CI actually lints, since CI checks out a clean tree — was green with only the known S7 warning. Two costs: (1) an agent or human reading the gate as red cannot tell a real finding from working-tree litter, and the honest workaround (lint the tracked set) is undocumented; (2) the same files were swept into a commit by a `git add -A` in this very cycle and had to be untracked in a follow-up — now mitigated by the `*.tmp.ts` entry added to `.gitignore` in this PR, but the lint blind spot remains for any other untracked file. Do NOT close this by adding ignore patterns for individual offenders — that is gate-weakening by attrition | `npm run lint` reports exactly the findings CI would report for the same commit, in a working tree containing arbitrary untracked files — either by making the flat config `.gitignore`-aware (`includeIgnoreFile`) or by scoping the lint script to the tracked/committed file set; verified by dropping a deliberately-failing untracked `.ts` file in the repo root and confirming `npm run lint` stays green while a committed copy of the same file turns it red. No rule is disabled and no per-file ignore is added. CLAUDE.md §2 documents the guarantee | open | no |
| S59 | P2 | `app/api/admin/categorize/route.ts:41` filters `servers.categories` (a `text[]`, `20260402000000_initial_schema.sql:52`) with `categories.eq.[]`, but `'[]'::text[]` is a malformed Postgres array literal — the correct empty-array literal is `{}`. Compounding it, the route discards the query `error` at `:38-48`, so a filter that fails outright reads to the operator as "All servers already categorized" rather than as a broken endpoint. The same predicate written correctly is now in `bots/extract-install-info.ts` (`categories.eq.{}`, validated against prod with a head-only count), so this is a straight divergence, not a judgement call (found 2026-07-31 by the filter-syntax review of the egress work) | `categorize` uses `categories.eq.{}`; the route surfaces a query error instead of swallowing it into a success message; a manual admin run against prod reports a category count consistent with the same predicate run directly against PostgREST | open | no |
| S60 | P2 | `/servers` shares the S35 defect that was only ever fixed for `/category/[category]`: `app/servers/page.tsx:36-40,53-57` awaits `searchParams` before any fetch, which promotes the route to fully dynamic, so its `revalidate = 60` export (`:19`) is INERT and every request — every crawler hit, every `?sort=`/`?page=`/`?min_score=` permutation — runs a live anon query with no cache absorption. The stale comment at `:152` still describes the export as if it worked ("revalidate=60 re-tries within a minute"), which is actively misleading: an egress plan drafted 2026-07-31 proposed raising 60→300 and would have shipped a pure no-op. Prod evidence is already recorded under S35 (`cache-control: private, no-store`, `x-vercel-cache: MISS`, absent from the prerender manifest) | The `/servers` listing query is wrapped in a tagged `unstable_cache` the way `app/page.tsx:95-99` and `app/security/page.tsx:92` already do (the pattern PR #82 applied to `/category`), verified by `x-vercel-cache` no longer reporting MISS/no-store on a repeat request to `/servers`; the misleading comment at `:152` is corrected or deleted so the next reader is not invited to tune a dead knob | open | no |
| S61 | P3 | Six full-table dedupe walks exist only because `servers` has no unique index on its natural keys. `bots/discover.ts` walks the whole table to build slug/github_url/npm_package/pip_package sets (collapsed from four walks to one on 2026-07-31, but still a full walk of 65,744 rows every run) and `bots/sync-registry.ts:87-95` does the same for `registry_id` and `github_url` — the header comment at `discover.ts:45-49` documents that a single swallowed page error re-inserts every known server past that point as a duplicate, i.e. the dedupe correctness rests entirely on these reads completing. Unique indexes plus `upsert(..., { ignoreDuplicates: true })` would delete all of them and remove the failure class outright, at the cost of a two-part migration: existing duplicates must be cleaned before any unique index can be created, and `detect-duplicates` currently resolves them by archiving, which can pick the wrong row (see S52) | `servers` has unique indexes on the natural keys (`lower(github_url)`, `slug`, partial uniques on `npm_package`/`pip_package`, `registry_id`), created after a duplicate-cleanup migration whose row counts are recorded; `discover` and `sync-registry` insert via `upsert` with `ignoreDuplicates` and no longer walk the table to dedupe; a run against prod inserts zero duplicates with the dedupe walks removed. Touches `supabase/migrations/**` (protected) | open | yes |
| S62 | P3 | The MCP `changes` tool (`app/api/mcp/route.ts:384-397`) is `.gt('score_computed_at', since)`, so it answers "which servers were RESCORED since X", not "which servers CHANGED since X" — the two coincided while compute-scores swept the fleet daily, but as of 2026-07-31 that bot rotates on a 7-day staleness window, so a server whose metadata changed can now go up to a week before surfacing to MCP consumers, and a server that changed nothing still surfaces the day it happens to be rescored. `servers.updated_at` is not the fix on its own: a trigger bumps it on every update (`20260402000000_initial_schema.sql:283-285`) and `bots/update-metadata.ts:114-137` rewrites every github-having row daily with no diff check, so it reads as "everything changed today" (found 2026-07-31 by the bot-behavior review of the egress work) | The `changes` tool is keyed on something that actually tracks change — the natural candidate is the existing `server_changes` audit table — or its description states plainly that it reports rescoring events, so an MCP consumer is not misled about freshness; a query against prod returns a change list that does not simply mirror yesterday's scoring batch | open | no |
