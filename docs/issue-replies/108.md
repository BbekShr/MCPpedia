## Reply draft for issue #108 (com-gtm-api-linkedin-mcp)

---

Your read of `bots/sync-registry.ts` was exactly right — thank you for going that deep. Already-linked rows really were only getting `registry_synced_at`/`registry_verified` restamped; `transport` and the package fields never flowed through after the initial import.

I've corrected your listing's transport to `stdio` + `streamable-http` from the current registry record, and fixed the underlying bug so this stops happening for every listing in this situation, not just yours — PR #138. The registry-sync bot will now gap-fill `transport`/`npm_package`/`pip_package` on an already-linked row when the stored value is stale/empty, without ever overwriting a value someone already set.

- Transport corrected: PR #137, applied 2026-08-31
- Root cause fixed: PR #138
