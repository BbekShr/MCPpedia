## Reply draft for issue #130 (Palisade / email.palisade/palisade)

---

Thanks for flagging this, and for submitting from the Palisade account — that's helpful context.

I've updated the listing to match the current official registry record (`email.palisade/palisade@1.0.2`):

- Repository: `https://github.com/palisadeemail/dns-auditor` → `https://github.com/palisadeemail/palisade-mcp`
- npm package: `@palisadeemail/mcp`
- Transports: `stdio`, `streamable-http`
- Authentication: now correctly flagged as required
- Description pulled from the registry record

One difference from your request: I used the registry's own `websiteUrl` (`https://developer.palisade.email/docs/guide`) rather than `https://www.palisade.email/mcp` for the homepage field, since the registry entry is what we treat as the authoritative source for this data. Happy to point it at the product page instead if you'd rather — let me know and I'll update it.

I didn't set up a formal claim/ownership transfer on this pass (that's a separate flow from the data correction) — if you want to claim the listing so you can edit it directly going forward, use the "Claim this listing" option on the page and I'll approve it once it comes through.

Underlying cause, for transparency: our registry-sync bot only refreshes two bookkeeping fields on a listing it's already linked to a registry entry — it never re-pulls the repo URL, package name, or transport after the initial import, so a repo rename/transfer like this one just sits stale forever. That's fixed for all affected listings (not just this one) in PR #138.

- Data corrected: PR #137, applied 2026-08-31
- Root cause (linked rows never refresh): PR #138
