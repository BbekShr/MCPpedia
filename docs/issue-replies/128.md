## Reply draft for issue #128 (io-github-cappyeo-discord-mcp)

---

Thanks — updated from the current registry record (`io.github.cappyeo/discord-mcp@0.26.0`).

One small correction to your numbers: the registry's live description now says **209** typed tools, not 208 — looks like it ticked up between when you filed this and when I looked. Description, tagline, and category (now `communication`) are all pulled from that current record, and I've corrected the authentication flag too (the site was showing it as not requiring auth, which isn't right given the required `DISCORD_TOKEN`).

The `0 tools` / tool schema is a separate pipeline (our schema extractor, which reads READMEs) and I haven't fixed that in this pass — it's a genuinely different problem from the metadata above. You offered a machine-readable schema artifact, which would help a lot: if you can point me at a stable URL for it (or confirm `https://cappyeo.github.io/discord-mcp/tools/` is meant to be machine-parseable), I'll look at teaching the extractor to read it. No promises on timing yet, but I don't want that offer to get lost.

- Metadata corrected: PR #137, applied 2026-08-31
- Root cause (linked rows never refresh): PR #138
- Tool schema extraction: not yet addressed, tracked separately
